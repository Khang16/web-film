version: "3.9"

services:
  frontend-vue:
    image: node:20-alpine
    container_name: vue-vite-app
    working_dir: /app
    ports:
      - "5175:5173"  # thay đổi cổng
    volumes:
      - ..:/app
    command: sh -c "npm install && npm run dev -- --host"
    environment:
      - NODE_ENV=development